<Window x:Class="Motorcycle_Emission_Inspection_Management.VehicleOwner.BookingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Motorcycle_Emission_Inspection_Management.VehicleOwner"
        mc:Ignorable="d"
        Title="BookingView" Height="450" Width="800">
    <Grid Background="#F3F7F9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Lên lịch kiểm định khí thải" 
                   FontSize="20" FontWeight="Bold" Foreground="#2B4F81"
                   HorizontalAlignment="Center" Margin="0,10,0,10"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Top">

            <StackPanel>
                <TextBlock Text="Phương tiện:"/>
                <ComboBox ItemsSource="{Binding Vehicles}" 
                          SelectedItem="{Binding SelectedVehicle}" 
                          DisplayMemberPath="LicensePlate" />
            </StackPanel>

            <StackPanel>
                <TextBlock Text="Cơ sở kiểm định:"/>
                <ComboBox ItemsSource="{Binding InspectionStations}" 
                          SelectedItem="{Binding SelectedStation}" 
                          DisplayMemberPath="StationName"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <StackPanel>
                    <TextBlock Text="Ngày kiểm định:"/>
                    <DatePicker SelectedDate="{Binding InspectionDate}" />
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Giờ kiểm định:"/>
                    <ComboBox ItemsSource="{Binding AvailableHours}" 
                              SelectedItem="{Binding SelectedHour}"/>
                </StackPanel>
            </StackPanel>

        </StackPanel>

        <Button Grid.Row="2" Content="Gửi yêu cầu" Height="40" 
                Margin="0,20,0,0"
                Background="#2B4F81" Foreground="White" 
                FontWeight="Bold"
                Command="{Binding SubmitCommand}" />
    </Grid>
</Window>
