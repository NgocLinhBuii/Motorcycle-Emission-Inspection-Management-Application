<Window x:Class="Prn_Project.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng nhập" Height="300" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Email:" />
            <!-- Binding cho Email -->
            <TextBox x:Name="emailTextBox" Text="{Binding Email}" Width="250" />

            <TextBlock Text="Mật khẩu:" Margin="0,10,0,0"/>
            <!-- PasswordBox dùng với PasswordChanged event -->
            <PasswordBox x:Name="pwdBox" Width="250" Margin="0,5" PasswordChanged="PwdBox_PasswordChanged"/>

            <Button Content="Đăng nhập" Command="{Binding LoginCommand}" Margin="0,15,0,0"/>
        </StackPanel>
    </Grid>
</Window>
